/** Move the existing search controls, retaining their value and keyboard focus. */
export declare function useSearchDock(page: string): {
    root: import("react").RefObject<HTMLDivElement>;
    compact: boolean;
};
