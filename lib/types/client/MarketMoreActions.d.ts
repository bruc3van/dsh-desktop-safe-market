import { type ReactNode } from 'react';
/** Native disclosure keeps normal Tab navigation for links and actions. */
export declare function MarketMoreActions({ label, children }: {
    label: string;
    children: ReactNode;
}): import("react").JSX.Element;
